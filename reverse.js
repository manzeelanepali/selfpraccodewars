// complete the function that accepts the string parameter and reverse each word in string. All spaces in the string should be retained

// eg: " This is an example!"=> "siht si na !elpmaxe"
// eg : "Dobule Spaces "=> "elbuod secaps"

// pseudocode
let array = [8, 2, 3, 4];
let revaray = array.concat().reverse();

// for finding the reverse by creating the shallow copy

console.log(revaray);
